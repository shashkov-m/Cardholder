//
//  CardholderTests.swift
//  CardholderTests
//
//  Created by Max Shashkov on 29.05.2022.
//

import XCTest
@testable import Cardholder

class CardholderTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }
  
  func testProviderDefinition() {
    //given
//    let visa = Card(number: "4123000000000000", style: .bluePinkGradient)
//    let unionPay = Card(number: "6223000000000000", style: .bluePinkGradient)
//    let mastercard = Card(number: "5323000000000000", style: .bluePinkGradient)
//    let mir = Card(number: "2202000000000000", style: .bluePinkGradient)
//    let maestro = Card(number: "5018000000000000", style: .bluePinkGradient)
//    let jcb = Card(number: "35323000000000000", style: .bluePinkGradient)
//    //then
//    XCTAssertEqual(visa.provider, Card.Provider.visa)
//    XCTAssertEqual(mastercard.provider, Card.Provider.mastercard)
//    XCTAssertEqual(unionPay.provider, Card.Provider.unionpay)
//    XCTAssertEqual(mir.provider, Card.Provider.mir)
//    XCTAssertEqual(maestro.provider, Card.Provider.maestro)
//    XCTAssertEqual(jcb.provider, Card.Provider.jcb)
  }

}
